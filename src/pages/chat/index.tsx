import { ChatWindow } from '@/entities/chat/ui';

const ChatPage = () => {
  return (
    <div className={'flex justify-center items-center w-full flex-grow'}>
      <ChatWindow />
    </div>
  );
};

export default ChatPage;
